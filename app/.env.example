# RPC Configuration
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Oracle Secret Key (KEEP SECRET - used to sign balance data)
# Generate with: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
ORACLE_SECRET_KEY=

# Optional: Restrict to specific token contracts (comma-separated)  
SUPPORTED_TOKENS=0x...,0x...

# thirdweb Configuration
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_client_id_here

# Contract Configuration (update after deployment)
NEXT_PUBLIC_ZK_MINT_CONTRACT_ADDRESS=0x...